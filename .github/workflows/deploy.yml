name: Deploy

on:
  push:
    branches:
      - main
      - dev
      - uat

jobs:
  deploy-dev:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Deploy to Dev
        env:
          DEV_URL: ${{ secrets.DEV_URL }}
        run: |
          echo "Deploying to Dev environment at $DEV_URL"
          # Add your deployment script/commands here

  deploy-uat:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Deploy to UAT
        env:
          UAT_URL: ${{ secrets.UAT_URL }}
        run: |
          echo "Deploying to UAT environment at $UAT_URL"
          # Add your deployment script/commands here